# สรุปการอัปเดตระบบแสดงราคาต้นทุน

## สิ่งที่ทำเสร็จแล้ว:

### 1. การอัปเดตฐานข้อมูล (add_original_price_columns.sql)
- เพิ่มคอลัมน์ `original_price`, `original_subtotal`, `original_options` ในตาราง `order_items`
- เพิ่มคอลัมน์ `original_total_price` ในตาราง `orders`
- สร้าง Index เพื่อประสิทธิภาพการค้นหา
- อัปเดตข้อมูลเดิมโดยใช้สัดส่วน 85% (สมมติ GP 15%)

### 2. การอัปเดต Backend API (ordersController.js)
- อัปเดต `PostOrders` เพื่อรับและบันทึกข้อมูล `originalTotalPrices`
- อัปเดต INSERT queries เพื่อเพิ่มคอลัมน์ราคาต้นทุน
- อัปเดต `getOrderStatus` และ `getOrdersByCustomer` เพื่อดึงข้อมูลราคาต้นทุน

### 3. การอัปเดตหน้าร้านค้า (OrdersListPage.dart)
- แสดงราคาขายและราคาต้นทุนในแต่ละรายการอาหาร
- เพิ่มส่วนสรุปราคาที่แสดง:
  - ยอดอาหาร (ราคาขาย) 
  - ยอดอาหาร (ต้นทุน)
  - ค่าจัดส่ง
  - รวมทั้งหมด (ราคาขาย)
  - รวมทั้งหมด (ต้นทุน + ค่าส่ง)

### 4. การอัปเดต Dashboard (Dashboard_sales_models.dart & Dashboard_salesPage.dart)
- เพิ่มฟิลด์ `totalCostRevenue`, `avgCostOrderValue` ใน DailySummary, MonthlySummary, YearlySummary
- อัปเดตหน้า Dashboard เพื่อแสดง:
  - ยอดขาย (ราคาขาย) vs ยอดขาย (ต้นทุน)
  - กำไรรวม (ขาย - ต้นทุน)
  - ค่าเฉลี่ยราคาขายและราคาต้นทุน

## คำจำกัดความ:
- **price**: ราคาอาหารที่ยังไม่บวก%เพิ่ม (ราคาต้นทุน)
- **sell_price**: ราคาอาหารที่บวก%เพิ่มแล้ว (ราคาขาย)
- **options**: ตัวเลือกของอาหารที่ยังไม่บวก%เพิ่ม
- **sell_options**: ตัวเลือกของอาหารที่บวก%เพิ่มแล้ว
- **total_price**: ราคารวมที่บวกค่าส่ง (ราคาขาย)

## สิ่งที่ยังต้องทำ:
1. **รัน SQL Script**: ต้องรัน `add_original_price_columns.sql` ในฐานข้อมูล
2. **อัปเดต Frontend การสร้างออเดอร์**: ต้องแก้ไขหน้าสร้างออเดอร์เพื่อส่งข้อมูล `originalTotalPrices`
3. **อัปเดต Backend Analytics**: ต้องแก้ไข API Dashboard เพื่อคำนวณและส่งข้อมูลราคาต้นทุน
4. **ทดสอบระบบ**: ทดสอบการทำงานของระบบหลังจากอัปเดตฐานข้อมูล

## คำแนะนำในการใช้งาน:
1. สำรองฐานข้อมูลก่อนรัน SQL Script
2. รัน SQL Script ใน `add_original_price_columns.sql`
3. ทดสอบการสร้างออเดอร์และดูข้อมูลในหน้าร้านค้า
4. ตรวจสอบ Dashboard ว่าแสดงข้อมูลราคาต้นทุนถูกต้อง

ระบบจะช่วยให้เจ้าของร้านค้าเห็นข้อมูลทั้งราคาขาย (ที่ลูกค้าจ่าย) และราคาต้นทุน (ต้นทุนจริงของอาหาร) เพื่อวิเคราะห์กำไรได้ชัดเจนยิ่งขึ้น